useDynLib(casper)

import(IRanges, graphics)
importFrom(VGAM,vglm,betabinomial.ab,Coef)
importFrom(mgcv,gam,predict.gam,s)
importFrom(GenomicFeatures, transcripts, exonsBy, makeTranscriptDbFromUCSC)
importFrom(survival,Surv,survfit)

#export regular functions
export(calcDenovo, calcExp, createDenovoGenome, getDistrs, getIsland, genePlot, modelPrior, pathCounts, plotExpr, plotPriorAS, posprob, procBam, procGenome, relativeExpr, rmShortInserts, simReads, transcripts, variants, "variants<-")

exportClasses(annotatedGenome, pathCounts, procBam)
#export general methods
exportMethods("plot")




